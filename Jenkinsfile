pipeline {
    agent {
        label 'node1'
    }
    stages {
        stage('Build') {
            steps {
                sh 'touch file.txt'
                // Add your build steps here
            }
        }
        
    }
}
